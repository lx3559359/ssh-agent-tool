# ShellPilot v0.4.51 Conservative Security and Release Hardening Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在不触碰既有 SSH/SFTP/root 身份、提示符、缓存和恢复事务行为的前提下，修补已确认的依赖风险，补齐稳定版签名、Linux 特权协议、运行时翻译和打包失败传播门禁。

**Architecture:** 只在依赖清单、独立构建校验脚本、GitHub Actions 工作流和现有消息翻译入口上增加窄范围门禁。所有新行为先由可注入依赖的 Node 单元测试约束；平台相关逻辑由小型 Node 包装器隔离，PowerShell 只负责调用 Windows 原生签名 API。SSH/SFTP 连接、有效身份探测、受控 PTY、root 文件后端、目录缓存、提示符隐藏和恢复事务实现均不修改。

**Tech Stack:** Electron 41.x、Node.js 22、CommonJS `node:test`、React、现有 ShellPilot i18n、ShellJS、PowerShell Authenticode、GitHub Actions、Playwright Electron。

---

## 实施约束

- 工作目录固定为 `F:\SSH工具开发\.worktrees\release-v0.4.48-verify`，应用目录固定为 `apps/electerm-agent`。
- 当前分支固定为 `codex/v0.4.51-security-release-hardening`。
- 不暂存或修改用户已有的未跟踪文件 `docs/superpowers/plans/2026-09-01-lazy-privileged-staging.md`。
- 禁止修改 `src/client/components/sftp/privileged-file-*`、SFTP 身份缓存、受控 PTY、root 文件后端、提示符识别或恢复事务算法。
- 每个主题独立提交；完成一个主题并跑完该主题的测试后再开始下一个主题。
- 没有真实代码签名证书时，只能验证“未签名包会被稳定版门禁拒绝”，不能宣称已生成有效签名发布包。
- 不合并、不推送、不打标签、不创建 GitHub Release；这些动作必须在最终回归通过后由用户另行授权。

## Task 1：锁定安全依赖基线并升级 Electron 与 qs

**Files:**

- Create: `apps/electerm-agent/test/unit-ci/dependency-security-baseline.spec.js`
- Modify: `apps/electerm-agent/package.json`
- Modify: `apps/electerm-agent/package-lock.json`

- [ ] **Step 1：先写依赖安全基线测试**

创建 `test/unit-ci/dependency-security-baseline.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')

const appRoot = path.resolve(__dirname, '../..')

function parseVersion (value) {
  assert.match(value, /^\d+\.\d+\.\d+$/, `${value} must be an exact version`)
  return value.split('.').map(Number)
}

function compareVersions (left, right) {
  const leftParts = parseVersion(left)
  const rightParts = parseVersion(right)
  for (let index = 0; index < 3; index += 1) {
    if (leftParts[index] !== rightParts[index]) {
      return leftParts[index] - rightParts[index]
    }
  }
  return 0
}

test('pins Electron to the reviewed 41.x security baseline', () => {
  const packageJson = JSON.parse(fs.readFileSync(
    path.join(appRoot, 'package.json'),
    'utf8'
  ))
  const electronVersion = packageJson.devDependencies.electron

  assert.equal(compareVersions(electronVersion, '41.10.7') >= 0, true)
  assert.equal(compareVersions(electronVersion, '42.0.0') < 0, true)
})

test('locks every qs instance to the compatible 6.x security baseline', () => {
  const packageLock = JSON.parse(fs.readFileSync(
    path.join(appRoot, 'package-lock.json'),
    'utf8'
  ))
  const qsVersions = Object.entries(packageLock.packages)
    .filter(([packagePath]) => (
      packagePath.replaceAll('\\', '/').endsWith('node_modules/qs')
    ))
    .map(([, metadata]) => metadata.version)

  assert.ok(qsVersions.length > 0, 'package-lock.json must contain qs')
  for (const version of qsVersions) {
    assert.equal(compareVersions(version, '6.16.0') >= 0, true)
    assert.equal(compareVersions(version, '7.0.0') < 0, true)
  }
})
```

- [ ] **Step 2：运行新测试并确认它因旧版本失败**

Run:

```powershell
Set-Location apps/electerm-agent
node --test test/unit-ci/dependency-security-baseline.spec.js
```

Expected: Electron `41.2.0` 或 qs `6.15.3` 触发断言失败；不能出现语法错误或文件路径错误。

- [ ] **Step 3：仅更新批准范围内的锁定版本**

Run:

```powershell
npm install --save-dev --save-exact electron@41.10.7 --package-lock-only
npm update qs --package-lock-only
git diff -- package.json package-lock.json
```

检查差异必须满足：

- `package.json` 中 Electron 精确为 `41.10.7`，没有 `^` 或 `~`。
- `package-lock.json` 中 Electron、`@electron/get`/`extract-zip` 等由 npm 为该升级正常重解析的节点可以变化。
- 所有 `qs` 节点至少为 `6.16.0` 且仍是 6.x。
- 不新增顶层 `qs` 依赖，不修改 Express 配置，不升级 Electron 主版本。
- 如果 npm 顺带改动无关顶层依赖，停止并恢复这些无关锁文件块，不把它们混入本提交。

- [ ] **Step 4：重装依赖并跑定向验证**

Run:

```powershell
npm ci
node --test test/unit-ci/dependency-security-baseline.spec.js
npm audit --omit=dev
npm audit
```

Expected:

- 基线测试 2 项通过。
- 生产依赖审计不再报告当前 qs 公告。
- 完整审计不再把 Electron 41.2.0 列为直接高危项；构建链的剩余公告逐项记录，不能误报为已清零。

- [ ] **Step 5：提交依赖主题**

Run:

```powershell
git add apps/electerm-agent/package.json apps/electerm-agent/package-lock.json apps/electerm-agent/test/unit-ci/dependency-security-baseline.spec.js
git diff --cached --check
git commit -m "build: update reviewed security baselines"
```

## Task 2：为稳定版增加 Windows Authenticode 签名门禁

**Files:**

- Create: `apps/electerm-agent/build/bin/verify-windows-authenticode.ps1`
- Create: `apps/electerm-agent/build/bin/verify-windows-authenticode.js`
- Create: `apps/electerm-agent/test/unit-ci/windows-authenticode.spec.js`
- Create: `apps/electerm-agent/test/unit-ci/windows-release-signing-gate.spec.js`
- Modify: `apps/electerm-agent/package.json`
- Modify: `.github/workflows/windows-electerm-agent-release.yml`

- [ ] **Step 1：先写 Node 包装器与发布工作流的失败测试**

创建 `test/unit-ci/windows-authenticode.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const path = require('node:path')

const {
  buildAuthenticodeArgs,
  resolveDefaultAuthenticodeTargets,
  verifyWindowsAuthenticode
} = require('../../build/bin/verify-windows-authenticode')

test('passes an explicit file list to the fixed PowerShell verifier', () => {
  const args = buildAuthenticodeArgs({
    scriptPath: 'C:\\repo\\verify.ps1',
    filePaths: ['C:\\dist\\ShellPilot.exe', 'C:\\dist\\installer.exe']
  })

  assert.deepEqual(args, [
    '-NoProfile',
    '-NonInteractive',
    '-ExecutionPolicy',
    'Bypass',
    '-File',
    'C:\\repo\\verify.ps1',
    'C:\\dist\\ShellPilot.exe',
    'C:\\dist\\installer.exe'
  ])
})

test('resolves exactly the unpacked app and versioned installer by default', () => {
  const cwd = path.resolve('fixture-app')
  assert.deepEqual(resolveDefaultAuthenticodeTargets({
    cwd,
    version: '0.4.51'
  }), [
    path.resolve(cwd, 'dist/win-unpacked/ShellPilot.exe'),
    path.resolve(cwd, 'dist/ShellPilot-0.4.51-win-x64-installer.exe')
  ])
})

test('refuses to run Authenticode verification outside Windows', () => {
  assert.throws(() => verifyWindowsAuthenticode({
    platform: 'linux',
    filePaths: ['ShellPilot.exe'],
    spawn: () => assert.fail('PowerShell must not be started')
  }), /Windows only/)
})

test('propagates PowerShell errors and invalid signatures', () => {
  assert.throws(() => verifyWindowsAuthenticode({
    platform: 'win32',
    filePaths: ['ShellPilot.exe'],
    scriptPath: 'verify.ps1',
    spawn: () => ({ status: 1, stdout: '', stderr: 'NotSigned' })
  }), /Authenticode verification failed/)
})

test('accepts a successful PowerShell verification', () => {
  let invocation
  const result = verifyWindowsAuthenticode({
    platform: 'win32',
    filePaths: ['ShellPilot.exe'],
    scriptPath: 'verify.ps1',
    spawn: (command, args, options) => {
      invocation = { command, args, options }
      return { status: 0, stdout: 'Valid', stderr: '' }
    }
  })

  assert.equal(result, 'Valid')
  assert.equal(invocation.command, 'powershell.exe')
  assert.equal(invocation.options.windowsHide, true)
})
```

创建 `test/unit-ci/windows-release-signing-gate.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')

const workflowPath = path.resolve(
  __dirname,
  '../../../../.github/workflows/windows-electerm-agent-release.yml'
)

test('stable Windows releases require credentials and verify both signatures', () => {
  const source = fs.readFileSync(workflowPath, 'utf8')
  const credentialGate = source.indexOf(
    'name: Require Windows signing credentials for stable release'
  )
  const installerBuild = source.indexOf('name: Build NSIS installer')
  const signatureGate = source.indexOf('name: Verify stable release signatures')
  const assetPreparation = source.indexOf('name: Prepare approved online update assets')

  assert.match(source, /CSC_LINK: \$\{\{ secrets\.WINDOWS_CSC_LINK \}\}/)
  assert.match(source, /CSC_KEY_PASSWORD: \$\{\{ secrets\.WINDOWS_CSC_KEY_PASSWORD \}\}/)
  assert.match(source, /release_channel == 'stable'/)
  assert.match(source, /npm run release:windows:verify-signatures/)
  assert.equal(credentialGate >= 0, true)
  assert.equal(credentialGate < installerBuild, true)
  assert.equal(installerBuild < signatureGate, true)
  assert.equal(signatureGate < assetPreparation, true)
})
```

- [ ] **Step 2：运行测试并确认缺少实现与工作流门禁**

Run:

```powershell
node --test test/unit-ci/windows-authenticode.spec.js test/unit-ci/windows-release-signing-gate.spec.js
```

Expected: 缺少 `verify-windows-authenticode` 模块，或工作流签名断言失败。

- [ ] **Step 3：实现固定目标的 PowerShell Authenticode 校验**

创建 `build/bin/verify-windows-authenticode.ps1`：

```powershell
[CmdletBinding()]
param(
  [Parameter(Mandatory = $true, Position = 0)]
  [ValidateNotNullOrEmpty()]
  [string[]]$Paths
)

$results = foreach ($targetPath in $Paths) {
  if (-not (Test-Path -LiteralPath $targetPath -PathType Leaf)) {
    throw "Authenticode target does not exist: $targetPath"
  }

  $resolvedPath = (Resolve-Path -LiteralPath $targetPath).Path
  $signature = Get-AuthenticodeSignature -LiteralPath $resolvedPath
  [PSCustomObject]@{
    Path = $resolvedPath
    Status = [string]$signature.Status
    Signer = if ($signature.SignerCertificate) {
      $signature.SignerCertificate.Subject
    } else {
      ''
    }
  }
}

$results | Format-Table -AutoSize | Out-String | Write-Host
$invalid = @($results | Where-Object { $_.Status -ne 'Valid' })
if ($invalid.Count -gt 0) {
  throw "Authenticode verification failed for $($invalid.Count) file(s)."
}
```

- [ ] **Step 4：实现可测试的 Node 包装器**

创建 `build/bin/verify-windows-authenticode.js`：

```js
const path = require('node:path')
const { spawnSync } = require('node:child_process')
const pack = require('../../package.json')

function buildAuthenticodeArgs ({ scriptPath, filePaths }) {
  return [
    '-NoProfile',
    '-NonInteractive',
    '-ExecutionPolicy',
    'Bypass',
    '-File',
    scriptPath,
    ...filePaths
  ]
}

function resolveDefaultAuthenticodeTargets ({
  cwd = process.cwd(),
  version = pack.version
} = {}) {
  return [
    path.resolve(cwd, 'dist/win-unpacked/ShellPilot.exe'),
    path.resolve(cwd, `dist/ShellPilot-${version}-win-x64-installer.exe`)
  ]
}

function verifyWindowsAuthenticode ({
  filePaths = resolveDefaultAuthenticodeTargets(),
  platform = process.platform,
  powershell = 'powershell.exe',
  scriptPath = path.resolve(__dirname, 'verify-windows-authenticode.ps1'),
  spawn = spawnSync
} = {}) {
  if (platform !== 'win32') {
    throw new Error('Authenticode verification is Windows only.')
  }
  if (!Array.isArray(filePaths) || filePaths.length === 0 ||
      filePaths.some(filePath => typeof filePath !== 'string' || !filePath)) {
    throw new Error('Authenticode verification requires explicit file paths.')
  }

  const result = spawn(
    powershell,
    buildAuthenticodeArgs({ scriptPath, filePaths }),
    { encoding: 'utf8', windowsHide: true }
  )
  if (result.stdout) process.stdout.write(result.stdout)
  if (result.stderr) process.stderr.write(result.stderr)
  if (result.error) throw result.error
  if (result.status !== 0) {
    throw new Error(`Authenticode verification failed with exit code ${result.status}.`)
  }
  return result.stdout || ''
}

function main () {
  verifyWindowsAuthenticode()
}

if (require.main === module) {
  main()
}

module.exports = {
  buildAuthenticodeArgs,
  main,
  resolveDefaultAuthenticodeTargets,
  verifyWindowsAuthenticode
}
```

在 `package.json` 的 release 脚本组中增加：

```json
"release:windows:verify-signatures": "node build/bin/verify-windows-authenticode.js"
```

- [ ] **Step 5：把签名秘密与稳定版门禁接入发布工作流**

在 `build-windows.env` 中增加 electron-builder 标准变量映射：

```yaml
      CSC_LINK: ${{ secrets.WINDOWS_CSC_LINK }}
      CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CSC_KEY_PASSWORD }}
```

在人工发布确认之后、依赖安装之前增加稳定版凭据检查：

```yaml
      - name: Require Windows signing credentials for stable release
        if: ${{ github.event.inputs.release_channel == 'stable' }}
        shell: pwsh
        env:
          WINDOWS_CSC_LINK: ${{ secrets.WINDOWS_CSC_LINK }}
          WINDOWS_CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CSC_KEY_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_CSC_LINK) -or
              [string]::IsNullOrWhiteSpace($env:WINDOWS_CSC_KEY_PASSWORD)) {
            throw "Stable releases require WINDOWS_CSC_LINK and WINDOWS_CSC_KEY_PASSWORD."
          }
```

在 `Build NSIS installer` 之后、包冒烟和资产准备之前增加：

```yaml
      - name: Verify stable release signatures
        if: ${{ github.event.inputs.release_channel == 'stable' }}
        run: npm run release:windows:verify-signatures
```

Beta 保持可在没有签名秘密时构建；稳定版缺少秘密或任一目标不是 `Valid` 时必须在上传前失败。

- [ ] **Step 6：跑定向测试并提交签名主题**

Run:

```powershell
node --test test/unit-ci/windows-authenticode.spec.js test/unit-ci/windows-release-signing-gate.spec.js
npm run lint
git add .github/workflows/windows-electerm-agent-release.yml apps/electerm-agent/package.json apps/electerm-agent/build/bin/verify-windows-authenticode.js apps/electerm-agent/build/bin/verify-windows-authenticode.ps1 apps/electerm-agent/test/unit-ci/windows-authenticode.spec.js apps/electerm-agent/test/unit-ci/windows-release-signing-gate.spec.js
git diff --cached --check
git commit -m "build: gate stable releases on valid signatures"
```

Expected: 定向测试通过，lint 通过；未签名的本地 `dist` 不能作为稳定版签名成功证据。

## Task 3：在 Ubuntu CI 中执行 Linux/root 特权文件协议测试

**Files:**

- Create: `apps/electerm-agent/build/bin/run-linux-privileged-file-tests.js`
- Create: `apps/electerm-agent/test/unit-ci/linux-privileged-file-test-runner.spec.js`
- Create: `apps/electerm-agent/test/unit-ci/linux-privileged-file-workflow.spec.js`
- Modify: `apps/electerm-agent/package.json`
- Modify: `.github/workflows/windows-electerm-agent-ci.yml`

- [ ] **Step 1：先写环境前置校验与工作流合同测试**

创建 `test/unit-ci/linux-privileged-file-test-runner.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const path = require('node:path')

const {
  assertLinuxRoot,
  runLinuxPrivilegedFileTests
} = require('../../build/bin/run-linux-privileged-file-tests')

test('requires Linux and UID 0', () => {
  assert.throws(() => assertLinuxRoot({
    platform: 'win32',
    getuid: () => 0
  }), /Linux/)
  assert.throws(() => assertLinuxRoot({
    platform: 'linux',
    getuid: () => 1000
  }), /UID 0/)
  assert.doesNotThrow(() => assertLinuxRoot({
    platform: 'linux',
    getuid: () => 0
  }))
})

test('runs only the privileged file protocol suite with the current Node', () => {
  let invocation
  const cwd = path.resolve('fixture-app')
  const status = runLinuxPrivilegedFileTests({
    cwd,
    platform: 'linux',
    getuid: () => 0,
    nodePath: '/opt/node/bin/node',
    spawn: (command, args, options) => {
      invocation = { command, args, options }
      return { status: 0 }
    }
  })

  assert.equal(status, 0)
  assert.equal(invocation.command, '/opt/node/bin/node')
  assert.deepEqual(invocation.args, [
    '--test',
    path.join(cwd, 'test/unit-ci/privileged-file-protocol.spec.js')
  ])
  assert.equal(invocation.options.cwd, cwd)
  assert.equal(invocation.options.stdio, 'inherit')
})

test('propagates the privileged suite exit code', () => {
  assert.throws(() => runLinuxPrivilegedFileTests({
    platform: 'linux',
    getuid: () => 0,
    spawn: () => ({ status: 3 })
  }), /exit code 3/)
})
```

创建 `test/unit-ci/linux-privileged-file-workflow.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')

test('CI runs the privileged file protocol suite as root on Ubuntu', () => {
  const source = fs.readFileSync(path.resolve(
    __dirname,
    '../../../../.github/workflows/windows-electerm-agent-ci.yml'
  ), 'utf8')

  assert.match(source, /linux-privileged-file-tests:/)
  assert.match(source, /runs-on: ubuntu-latest/)
  assert.match(source, /sudo "\$\(command -v node\)" build\/bin\/run-linux-privileged-file-tests\.js/)
  assert.match(source, /cache-dependency-path: apps\/electerm-agent\/package-lock\.json/)
})
```

- [ ] **Step 2：运行测试并确认它因缺少 runner 和 CI 作业失败**

Run:

```powershell
node --test test/unit-ci/linux-privileged-file-test-runner.spec.js test/unit-ci/linux-privileged-file-workflow.spec.js
```

Expected: 缺少 runner 模块或缺少 Ubuntu 作业导致失败。

- [ ] **Step 3：实现 Linux/root runner**

创建 `build/bin/run-linux-privileged-file-tests.js`：

```js
const path = require('node:path')
const { spawnSync } = require('node:child_process')

function assertLinuxRoot ({
  platform = process.platform,
  getuid = process.getuid
} = {}) {
  if (platform !== 'linux') {
    throw new Error('Privileged file protocol CI must run on Linux.')
  }
  if (typeof getuid !== 'function' || getuid() !== 0) {
    throw new Error('Privileged file protocol CI must run as UID 0.')
  }
}

function runLinuxPrivilegedFileTests ({
  cwd = process.cwd(),
  platform = process.platform,
  getuid = process.getuid,
  nodePath = process.execPath,
  spawn = spawnSync
} = {}) {
  assertLinuxRoot({ platform, getuid })
  const testPath = path.join(
    cwd,
    'test/unit-ci/privileged-file-protocol.spec.js'
  )
  const result = spawn(nodePath, ['--test', testPath], {
    cwd,
    stdio: 'inherit'
  })
  if (result.error) throw result.error
  if (result.status !== 0) {
    throw new Error(
      `Privileged file protocol tests failed with exit code ${result.status}.`
    )
  }
  return result.status
}

if (require.main === module) {
  runLinuxPrivilegedFileTests()
}

module.exports = {
  assertLinuxRoot,
  runLinuxPrivilegedFileTests
}
```

在 `package.json` 增加：

```json
"test-linux-privileged-file-ci": "node build/bin/run-linux-privileged-file-tests.js"
```

- [ ] **Step 4：增加独立 Ubuntu CI 作业**

在 `.github/workflows/windows-electerm-agent-ci.yml` 的 `jobs` 下保留现有 Windows 作业，并增加：

```yaml
  linux-privileged-file-tests:
    name: Run Linux root privileged file tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/electerm-agent

    env:
      CI: true
      WORKFLOW_NAME: linux-privileged-file-ci

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/electerm-agent/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run privileged file protocol tests as UID 0
        run: sudo "$(command -v node)" build/bin/run-linux-privileged-file-tests.js
```

`sudo` 只提升当前 `node` 进程；不把 npm 缓存或工作区递归改权。测试自身仍可因扩展元数据能力缺失显式跳过，但不能再因平台或 UID 不是 0 跳过。

- [ ] **Step 5：跑定向测试并提交 Linux CI 主题**

Run:

```powershell
node --test test/unit-ci/linux-privileged-file-test-runner.spec.js test/unit-ci/linux-privileged-file-workflow.spec.js
npm run lint
git add .github/workflows/windows-electerm-agent-ci.yml apps/electerm-agent/package.json apps/electerm-agent/build/bin/run-linux-privileged-file-tests.js apps/electerm-agent/test/unit-ci/linux-privileged-file-test-runner.spec.js apps/electerm-agent/test/unit-ci/linux-privileged-file-workflow.spec.js
git diff --cached --check
git commit -m "ci: exercise privileged file protocol on Linux"
```

## Task 4：把审计定位的核心提示接入中英文运行时翻译

**Files:**

- Modify: `apps/electerm-agent/src/client/common/shellpilot-i18n-overrides.js`
- Modify: `apps/electerm-agent/src/client/components/sftp/sftp-entry.jsx`
- Modify: `apps/electerm-agent/src/client/components/terminal/terminal.jsx`
- Modify: `apps/electerm-agent/src/client/components/main/upgrade.jsx`
- Modify: `apps/electerm-agent/test/unit-ci/shellpilot-i18n-overrides.spec.js`
- Modify: `apps/electerm-agent/test/unit-ci/update-manual-feedback.spec.js`
- Create: `apps/electerm-agent/test/unit-ci/core-message-i18n.spec.js`

- [ ] **Step 1：先扩充双语目录期望值**

在 `shellpilot-i18n-overrides.spec.js` 的 `expectedCatalogs.zh_cn` 和 `expectedCatalogs.en_us` 中分别增加以下键值：

```js
// zh_cn
shellpilotSftpSelectRemoteTargets: '请先在远程 SFTP 面板选择文件或文件夹。',
shellpilotSftpBackupUncertain: '远端备份可能已完成，但恢复记录未能持久化；请先核对再继续。',
shellpilotSftpBackupFailedUnchanged: 'SFTP 备份失败，原文件未改动。',
shellpilotSftpReconnectBeforeRestore: '请先连接服务器 {host} 后再恢复。',
shellpilotSftpRestoreUncertain: '恢复结果不确定；恢复前内容可能位于记录的 displaced 路径，请在安全中心核对。',
shellpilotSftpRestoreFailedVerify: '恢复失败；远端内容未宣告安全，请在重试前核对恢复记录。',
shellpilotSftpRestoreCompletedPreserved: '恢复完成；恢复前的当前内容也已另行保留。',
shellpilotSftpNoRecoveryRecord: '当前文件没有可用的备份或安全删除记录。',
shellpilotTerminalUnsafeFilename: '文件名包含不安全字符',
shellpilotTerminalTransferInProgress: '已有传输任务正在进行',
shellpilotUpdateUnavailableOnline: '当前版本暂时不能在线更新。',
shellpilotUpdateDownloadUnavailable: '该版本暂时不能在线更新。',
shellpilotUpdateDownloadIncomplete: '更新文件尚未下载完成，请稍后重试。',
shellpilotUpdateDownloadedRestart: '更新已下载完成，重启客户端即可完成更新。',
shellpilotUpdateOnlineFailed: '在线更新失败，请稍后重试。',
shellpilotUpdateCheckFailed: '检查更新失败，请稍后重试。'
```

```js
// en_us
shellpilotSftpSelectRemoteTargets: 'Select files or folders in the remote SFTP panel first.',
shellpilotSftpBackupUncertain: 'The remote backup may have completed, but its recovery record could not be saved. Verify it before continuing.',
shellpilotSftpBackupFailedUnchanged: 'SFTP backup failed. The original files were not changed.',
shellpilotSftpReconnectBeforeRestore: 'Connect to {host} before restoring.',
shellpilotSftpRestoreUncertain: 'The restore result is uncertain. The pre-restore content may be at the displaced path in the record; review it in the Safety Center.',
shellpilotSftpRestoreFailedVerify: 'Restore failed. The remote content is not declared safe; review the recovery record before retrying.',
shellpilotSftpRestoreCompletedPreserved: 'Restore completed. The content that existed before the restore was also preserved.',
shellpilotSftpNoRecoveryRecord: 'No backup or safe-delete record is available for this file.',
shellpilotTerminalUnsafeFilename: 'The file name contains unsafe characters',
shellpilotTerminalTransferInProgress: 'A transfer is already in progress',
shellpilotUpdateUnavailableOnline: 'This version cannot be updated online right now.',
shellpilotUpdateDownloadUnavailable: 'This update cannot be downloaded online right now.',
shellpilotUpdateDownloadIncomplete: 'The update has not finished downloading. Try again later.',
shellpilotUpdateDownloadedRestart: 'The update has downloaded. Restart the client to finish installing it.',
shellpilotUpdateOnlineFailed: 'Online update failed. Try again later.',
shellpilotUpdateCheckFailed: 'Unable to check for updates. Try again later.'
```

- [ ] **Step 2：先写组件源码合同并修正旧的更新反馈断言**

创建 `test/unit-ci/core-message-i18n.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')

function readClientSource (relativePath) {
  return fs.readFileSync(path.resolve(
    __dirname,
    '../../src/client/components',
    relativePath
  ), 'utf8')
}

test('SFTP recovery messages use runtime translation keys', () => {
  const source = readClientSource('sftp/sftp-entry.jsx')
  const keys = [
    'shellpilotSftpSelectRemoteTargets',
    'shellpilotSftpBackupUncertain',
    'shellpilotSftpBackupFailedUnchanged',
    'shellpilotSftpReconnectBeforeRestore',
    'shellpilotSftpRestoreUncertain',
    'shellpilotSftpRestoreFailedVerify',
    'shellpilotSftpRestoreCompletedPreserved',
    'shellpilotSftpNoRecoveryRecord'
  ]
  for (const key of keys) assert.match(source, new RegExp(key))
  assert.doesNotMatch(source, /请先在远程 SFTP 面板选择文件或文件夹。/)
  assert.doesNotMatch(source, /远端备份可能已完成，但恢复记录未能持久化/)
  assert.doesNotMatch(source, /SFTP 备份失败，原文件未改动。/)
  assert.doesNotMatch(source, /请先连接服务器 \$\{record\.host\} 后再恢复。/)
  assert.doesNotMatch(source, /恢复结果不确定；恢复前内容可能位于记录的 displaced 路径/)
  assert.doesNotMatch(source, /恢复失败；远端内容未宣告安全/)
  assert.doesNotMatch(source, /恢复完成；恢复前的当前内容也已另行保留。/)
  assert.doesNotMatch(source, /当前文件没有可用的备份或安全删除记录。/)
})

test('terminal drop and transfer messages use runtime translation keys', () => {
  const source = readClientSource('terminal/terminal.jsx')
  assert.match(source, /e\('shellpilotTerminalUnsafeFilename'\)/)
  assert.match(source, /e\('shellpilotTerminalTransferInProgress'\)/)
  assert.doesNotMatch(source, /File name contains unsafe characters/)
  assert.doesNotMatch(source, /A transfer is already in progress/)
})

test('update fallbacks use runtime translation keys', () => {
  const source = readClientSource('main/upgrade.jsx')
  const keys = [
    'shellpilotUpdateChecking',
    'shellpilotUpdateUnavailableOnline',
    'shellpilotUpdateDownloadUnavailable',
    'shellpilotUpdateDownloadIncomplete',
    'shellpilotUpdateDownloadedRestart',
    'shellpilotUpdateOnlineFailed',
    'shellpilotUpdateCheckFailed'
  ]
  for (const key of keys) assert.match(source, new RegExp(key))
  assert.doesNotMatch(source, /正在检查更新\.\.\./)
  assert.doesNotMatch(source, /更新已下载完成，重启客户端即可完成更新。/)
  assert.doesNotMatch(source, /检查更新失败，请稍后重试。/)
})
```

把 `update-manual-feedback.spec.js` 中：

```js
assert.match(upgradeSource, /message\.info\('正在检查更新/)
```

替换为：

```js
assert.match(upgradeSource, /message\.info\(e\('shellpilotUpdateChecking'\), 0\)/)
```

- [ ] **Step 3：运行翻译测试并确认目录和组件仍未满足新合同**

Run:

```powershell
node --test test/unit-ci/shellpilot-i18n-overrides.spec.js test/unit-ci/core-message-i18n.spec.js test/unit-ci/update-manual-feedback.spec.js
```

Expected: 新目录键未定义、组件仍含硬编码文本，测试按这两个原因失败。

- [ ] **Step 4：在中英文目录增加完全对称的键值**

将 Step 1 的 16 个键分别加入 `shellpilot-i18n-overrides.js` 的 `zh_cn` 与 `en_us` 目录，放在现有 SFTP/更新键附近。不得删除或改名既有键。运行目录完整性测试，确保 `getShellPilotCatalogKeys('zh_cn')` 与 `en_us` 完全一致。

- [ ] **Step 5：仅替换已审计的 SFTP 文案，不改控制流**

保持所有 `if`、`catch`、返回值、恢复记录状态和调用顺序不变，只做以下等价替换：

```js
message.warning(e('shellpilotSftpSelectRemoteTargets'))

message.error(err?.code === 'REMOTE_FILE_RECOVERY_UNCERTAIN'
  ? e('shellpilotSftpBackupUncertain')
  : e('shellpilotSftpBackupFailedUnchanged'))

message.warning(formatShellPilotTranslation(
  e,
  'shellpilotSftpReconnectBeforeRestore',
  { host: record.host }
))

message.error(err?.code === 'REMOTE_FILE_RECOVERY_UNCERTAIN'
  ? e('shellpilotSftpRestoreUncertain')
  : e('shellpilotSftpRestoreFailedVerify'))

message.success(e('shellpilotSftpRestoreCompletedPreserved'))

message.info(e('shellpilotSftpNoRecoveryRecord'))
```

- [ ] **Step 6：仅替换终端和更新文案，不改交互行为**

在 `terminal.jsx` 中复用一个翻译后的字符串：

```js
const notSafeMsg = e('shellpilotTerminalUnsafeFilename')
```

`cd` 中直接使用：

```js
return message.error(e('shellpilotTerminalUnsafeFilename'))
```

三处分支全部替换为：

```js
message.warning(e('shellpilotTerminalTransferInProgress'))
```

在 `upgrade.jsx` 中保持远端返回的 `checked.message`、`downloadState.message`、`finalState.error` 和 `err.message` 优先级不变，只把中文 fallback 替换为：

```js
e('shellpilotUpdateUnavailableOnline')
e('shellpilotUpdateDownloadUnavailable')
e('shellpilotUpdateDownloadIncomplete')
e('shellpilotUpdateDownloadedRestart')
e('shellpilotUpdateOnlineFailed')
e('shellpilotUpdateChecking')
e('shellpilotUpdateCheckFailed')
```

- [ ] **Step 7：跑定向测试、lint 并提交翻译主题**

Run:

```powershell
node --test test/unit-ci/shellpilot-i18n-overrides.spec.js test/unit-ci/core-message-i18n.spec.js test/unit-ci/update-manual-feedback.spec.js
npm run lint
git add apps/electerm-agent/src/client/common/shellpilot-i18n-overrides.js apps/electerm-agent/src/client/components/sftp/sftp-entry.jsx apps/electerm-agent/src/client/components/terminal/terminal.jsx apps/electerm-agent/src/client/components/main/upgrade.jsx apps/electerm-agent/test/unit-ci/shellpilot-i18n-overrides.spec.js apps/electerm-agent/test/unit-ci/core-message-i18n.spec.js apps/electerm-agent/test/unit-ci/update-manual-feedback.spec.js
git diff --cached --check
git commit -m "fix: localize core runtime feedback"
```

## Task 5：让 Yarn 自动清理失败立即终止打包准备

**Files:**

- Create: `apps/electerm-agent/build/bin/prepare-command-utils.js`
- Create: `apps/electerm-agent/test/unit-ci/prepare-command-utils.spec.js`
- Modify: `apps/electerm-agent/build/bin/prepare.js`

- [ ] **Step 1：先写成功、非零返回和异常三类测试**

创建 `test/unit-ci/prepare-command-utils.spec.js`：

```js
const test = require('node:test')
const assert = require('node:assert/strict')

const {
  runRequiredShellCommand
} = require('../../build/bin/prepare-command-utils')

test('returns a successful ShellJS command result', () => {
  const expected = { code: 0 }
  const actual = runRequiredShellCommand({
    command: 'yarn autoclean --force',
    execCommand: () => expected,
    failureMessage: 'Yarn cleanup failed.'
  })
  assert.equal(actual, expected)
})

test('throws an actionable error for a non-zero ShellJS result', () => {
  assert.throws(() => runRequiredShellCommand({
    command: 'yarn autoclean --force',
    execCommand: () => ({ code: 127 }),
    failureMessage: 'Install Yarn Classic 1.22.22 and retry.'
  }), error => {
    assert.equal(error.code, 'PACKAGE_PREPARE_COMMAND_FAILED')
    assert.equal(error.exitCode, 127)
    assert.match(error.message, /Yarn Classic 1\.22\.22/)
    return true
  })
})

test('wraps command exceptions without hiding the cause', () => {
  const cause = new Error('spawn failed')
  assert.throws(() => runRequiredShellCommand({
    command: 'yarn autoclean --force',
    execCommand: () => { throw cause },
    failureMessage: 'Install Yarn Classic 1.22.22 and retry.'
  }), error => {
    assert.equal(error.code, 'PACKAGE_PREPARE_COMMAND_FAILED')
    assert.equal(error.cause, cause)
    return true
  })
})
```

- [ ] **Step 2：运行测试并确认 helper 尚不存在**

Run:

```powershell
node --test test/unit-ci/prepare-command-utils.spec.js
```

Expected: 因缺少 `prepare-command-utils` 模块失败。

- [ ] **Step 3：实现失败传播 helper**

创建 `build/bin/prepare-command-utils.js`：

```js
function createRequiredCommandError ({
  cause,
  command,
  exitCode,
  failureMessage
}) {
  const suffix = Number.isInteger(exitCode)
    ? ` Exit code: ${exitCode}.`
    : ''
  const error = new Error(`${failureMessage}${suffix}`)
  error.code = 'PACKAGE_PREPARE_COMMAND_FAILED'
  error.command = command
  error.exitCode = exitCode
  if (cause) error.cause = cause
  return error
}

function runRequiredShellCommand ({
  command,
  execCommand,
  failureMessage
}) {
  let result
  try {
    result = execCommand(command)
  } catch (cause) {
    throw createRequiredCommandError({
      cause,
      command,
      failureMessage
    })
  }
  if (!result || result.code !== 0) {
    throw createRequiredCommandError({
      command,
      exitCode: result?.code,
      failureMessage
    })
  }
  return result
}

module.exports = {
  runRequiredShellCommand
}
```

- [ ] **Step 4：只包裹现有 Yarn 自动清理命令**

在 `prepare.js` 顶部增加：

```js
const { runRequiredShellCommand } = require('./prepare-command-utils')
```

把原来的裸 `exec(...)` 替换为：

```js
runRequiredShellCommand({
  execCommand: exec,
  command: `cd work/app && yarn generate-lock-entry > yarn.lock && yarn autoclean --force && cd ${cwd}`,
  failureMessage: 'Package cleanup failed. Install Yarn Classic 1.22.22 and retry.'
})
```

该调用必须仍位于复制 `.yarnclean` 之后、删除 `.yarnclean` 和调用 `verify-runtime-package` 之前。非零返回后不得继续输出 `done pack prepare`。

- [ ] **Step 5：跑定向测试并提交打包失败传播主题**

Run:

```powershell
node --test test/unit-ci/prepare-command-utils.spec.js test/unit-ci/prepare-cleanup-utils.spec.js
npm run lint
git add apps/electerm-agent/build/bin/prepare-command-utils.js apps/electerm-agent/build/bin/prepare.js apps/electerm-agent/test/unit-ci/prepare-command-utils.spec.js
git diff --cached --check
git commit -m "build: fail package prepare when yarn cleanup fails"
```

## Task 6：执行完整回归、Playwright 界面自检与发布前审查

**Files:**

- Modify only if evidence requires it: files already listed in Tasks 1–5
- Do not create a release tag or release artifact manifest in this task

- [ ] **Step 1：确认工作区只包含本计划的提交和用户原有未跟踪文件**

Run:

```powershell
Set-Location F:\SSH工具开发\.worktrees\release-v0.4.48-verify
git status --short
git log --oneline --decorate -8
```

Expected: 业务文件无未提交改动；用户原有 `docs/superpowers/plans/2026-09-01-lazy-privileged-staging.md` 仍未跟踪且未被暂存。

- [ ] **Step 2：运行完整单元测试和静态构建验证**

Run:

```powershell
Set-Location apps/electerm-agent
npm run test-unit-ci
npm run lint
npm run vite-build
```

Expected:

- 单元测试 0 失败；Windows 上既有 Linux/root 条件跳过允许保留。
- lint 0 错误。
- Vite 构建成功；已有大分块警告可记录，但不能出现新的警告类别。

- [ ] **Step 3：验证 Yarn、完整打包准备和 Windows 目录包**

Run:

```powershell
yarn --version
npm run b
npm run package:win:dir
npm run pb
node -e "const c = require('./build/bin/build-common'); c.patchNsisKeepShortcuts(); c.writeSrc('win-x64-installer.exe')"
npx electron-builder --win nsis --publish never
npm run test-package-smoke
node build/bin/prepare-win-portable-ci.js
npx electron-builder --win zip --publish never
npm run verify-win-portable
```

Expected:

- Yarn 输出 `1.22.22`；若命令不存在，先运行 `npm install -g yarn@1.22.22`，再从 `yarn --version` 重新开始。
- `npm run b` 成功；Yarn 命令失败时必须出现安装 Yarn Classic 1.22.22 的可操作错误并返回非零。
- Windows 目录包、NSIS 安装包和 portable ZIP 都由当前提交重新生成，包冒烟与 portable 校验通过。

- [ ] **Step 4：运行本地可重复的 Playwright/Electron 核心场景**

Run:

```powershell
npx playwright test test/e2e/027.quality-core-flows.spec.js test/e2e/028.crash-recovery.spec.js test/e2e/039.operations-pty-identity.spec.js --workers=1
$env:SHELLPILOT_VISUAL_MATRIX_SMOKE = '1'
npx playwright test test/e2e/022.secondary-ui-visual-matrix.spec.js --workers=1
Remove-Item Env:SHELLPILOT_VISUAL_MATRIX_SMOKE
```

Expected:

- 质量核心、崩溃恢复、受控 PTY/身份与本地 SFTP 场景全部通过。
- 视觉矩阵 smoke 在现有最小视口、缩放、语言和主题下通过，不出现溢出、裁切或启动错误。
- 重点查看目录双击、终端提示符、SFTP 面板切换和更新反馈；不能因本轮翻译或依赖升级改变控件位置和操作顺序。

- [ ] **Step 5：只有在四个私有环境变量均已存在时运行真实 sudo/SFTP 场景**

Run:

```powershell
$required = @(
  'SHELLPILOT_E2E_HOST',
  'SHELLPILOT_E2E_PORT',
  'SHELLPILOT_E2E_USERNAME',
  'SHELLPILOT_E2E_PASSWORD'
)
$missing = $required | Where-Object { [string]::IsNullOrWhiteSpace((Get-Item "Env:$_" -ErrorAction SilentlyContinue).Value) }
if ($missing.Count -eq 0) {
  node test/e2e/run-041-private.js --reporter=line
} else {
  Write-Host "Private 041 test not run because its four credential variables are unavailable."
}
```

Expected: 凭据存在时测试通过并清理其临时账号；凭据不存在时明确记录未运行，不能把它写成“通过”。

- [ ] **Step 6：复核依赖审计、签名负向门禁和发布脚本**

Run:

```powershell
npm audit --omit=dev
npm audit
npm run release:prepare-assets
npm run release:local:verify
Get-AuthenticodeSignature -LiteralPath dist\win-unpacked\ShellPilot.exe | Select-Object Path, Status, StatusMessage
```

Expected:

- qs 公告不再出现；Electron 直接高危项不再出现。
- 本地发布资产校验通过。
- 没有真实证书的开发机上，`ShellPilot.exe` 可显示 `NotSigned`；随后运行 `npm run release:windows:verify-signatures` 必须返回非零。这是稳定版门禁的正确负向结果，不是已签名发布成功。
- 若开发机已配置真实证书，安装包和主程序必须都显示 `Valid`，签名脚本才可返回 0。

- [ ] **Step 7：对比禁改区并进行最终差异审查**

Run:

```powershell
Set-Location F:\SSH工具开发\.worktrees\release-v0.4.48-verify
git diff f8e13816a4c4f3a717ee55804bddff4703418243 --stat
git diff f8e13816a4c4f3a717ee55804bddff4703418243 -- apps/electerm-agent/src/client/components/sftp/privileged-file-protocol.js apps/electerm-agent/src/client/components/sftp/privileged-file-staging.js apps/electerm-agent/src/client/components/terminal/shell.js
git status --short
```

Expected:

- 禁改区差异为空。
- 最终差异只属于 Tasks 1–5 和两份已确认文档。
- 无空白错误、无凭据内容、无构建产物、无临时 Playwright profile 被暂存。

- [ ] **Step 8：请求代码审查，按证据修正后再做一次完整验证**

使用 `superpowers:requesting-code-review` 审查以下内容：

- 稳定版签名门禁是否在任何上传/Release 创建之前执行。
- Linux runner 是否真的要求 `linux` 和 UID 0，且只运行目标特权协议套件。
- i18n 改动是否只替换文本，没有改变异常分支、返回值或恢复状态。
- Yarn 失败是否停止后续清理和运行时包验证。
- Electron 是否保持 41.x 精确锁定，qs 是否保持兼容 6.x。

任何修正都先补失败测试，再以对应主题的 `fix:` 提交落地。修正完成后重新执行 Steps 2–7。

## 完成判定

只有同时满足以下条件，实施分支才可交回用户决定是否合并和发布：

- Tasks 1–5 的定向测试和完整 `npm run test-unit-ci` 全部 0 失败。
- lint、Vite 构建、Windows 目录打包与包冒烟通过。
- 本地 Playwright 核心、PTY/身份、SFTP 和视觉 smoke 通过；私有 041 场景按凭据可用性如实记录。
- qs 公告与 Electron 41.2.0 直接高危项消失，剩余审计项有准确记录。
- 稳定版发布缺少签名秘密时前置失败；安装包或主程序不是 `Valid` 时上传前失败。
- 现有 SSH/SFTP/root 身份、提示符、缓存和恢复事务实现没有业务差异。
- 分支保持独立、可审查、可逐主题回退；未执行合并、推送、标签或发布。
