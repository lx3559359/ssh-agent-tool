import quickConnect from '../../app/common/parse-quick-connect.js'

const {
  parseQuickConnect,
  getDefaultPort,
  getSupportedProtocols,
  SUPPORTED_PROTOCOLS,
  DEFAULT_PORTS,
  OPTS_DENY_LIST
} = quickConnect

export {
  parseQuickConnect,
  getDefaultPort,
  getSupportedProtocols,
  SUPPORTED_PROTOCOLS,
  DEFAULT_PORTS,
  OPTS_DENY_LIST
}
