# 环境变量配置示例
# 开发：复制为 .env.development.local（仅 next dev 加载，不会影响桌面打包）
# 也可使用 .env.local，但运行 npm run build:desktop 时会自动临时移走

# 后端 API 地址（开发环境需要配置）
NEXT_PUBLIC_API_URL=http://localhost:8000

# 后端 WebSocket 地址（开发环境需要配置）
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/terminal
