# Upstream

This directory is based on WinkTerm.

- Repository: https://github.com/Cznorth/winkterm
- License: MIT
- Initial import date: 2026-06-25
- Initial import commit: `2471cd5`
- Commit source: `external/winkterm-runtime`

Local product changes are added under `product/` first so upstream files remain easy to compare.

## Import Notes

The import intentionally preserves upstream files as-is, including `build/build.bat` and `build/winkterm.spec`, because they are relevant to Windows desktop packaging. Existing upstream whitespace warnings are left unchanged so the initial import remains comparable to the upstream commit.
